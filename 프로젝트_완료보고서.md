# GPT-2 바닥부터 구축하기: Cloze 기반 Paraphrase Detection의 파라미터 효율적 미세조정(LoRA·ReFT) 설계축 분리와 Sonnet 생성 디코딩 전략의 다중 시드 정량 분석

**주제 구분: 지정 주제 프로젝트 (GPT-2 구축하기)**

**팀원**
- 최연식 (chldustlr559@gmail.com)

**GitHub Repository**: https://github.com/yeonsik-choi/NLP_GPT2

---

## 초록 (Abstract)

**동인·목표.** 모델을 라이브러리로 호출하는 것과 내부를 직접 구현하는 것 사이에는 큰 이해의 격차가 있고, LoRA 같은 파라미터 효율적 미세조정(PEFT)은 "어디에·얼마나" 적용해야 하는지가 소규모 예산에서 불투명하며, 단일 GPU(8GB)라는 현실 제약과 단일 시드 실험의 신뢰성 문제는 흔히 간과된다. 본 프로젝트는 GPT-2(124M)를 PyTorch로 바닥부터 구축하고, 이를 Paraphrase Detection(Quora)과 Sonnet 생성(Shakespeare)에 적용·확장하면서 위 세 문제를 정량적으로 다룬다.

**방법.** PART-I에서 멀티헤드 인과 자기어텐션·pre-LayerNorm 블록·가중치 공유 출력층·AdamW를 직접 구현해 HuggingFace 공식 가중치 검증(`sanity_check.py`)을 통과시켰다. PART-II에서는 (1) Paraphrase Detection을 **Cloze 방식**("yes"/"no" 토큰 생성)으로 재구성하고, (2) **LoRA**[Hu et al., 2021]의 주입 모듈·rank·α·dropout·lr 축과 **ReFT**[Wu et al., 2024]를 동일 조건에서 ablation했으며, (3) Sonnet 디코딩 전략(temperature·top-k/p·repetition penalty·beam search)의 chrF 영향을 측정했다. 모든 헤드라인 주장은 **3-시드 paired 비교**로 재검증하고, 예제 단위 McNemar 검정·학습 곡선·퇴화 지표(distinct-n·rep-4)로 보강했다.

**주요 성과.** ① Cloze 재구성으로 1-epoch dev 정확도 **86.4%**(3-시드 86.0%±0.8)의 기준 모델을 확보했다. ② PEFT의 가치는 *정확도 동급*이 아니라 **효율**임을 다층적으로 규명했다: LoRA r=32는 0.94% 파라미터·2.1~2.2배 속도를 주지만, 동일-step에서도(−1.3%p) 1 epoch 수렴에서도(−1.7%p, 전 시드) full fine-tuning에 못 미쳐 "충분히 학습하면 따라잡는다"는 가설을 기각했다. ③ 설계축의 위계를 분리했다: 모듈 확대(노이즈성) < rank 확대(+2.4%p, 견고) < **α(scaling) 상향**(α=64/r=8이 **1/4 파라미터로 r=32와 동급**) — 즉 rank로 귀속되던 이점의 상당 부분은 유효 업데이트 크기(α/r) 효과다. 단 α×lr 2D sweep에서 α와 lr은 곱으로 환원되지 않는 별개 축이고, 수렴 영역에서는 α/r 효과가 사라져 초기 적응 가속에 국한됨도 보였다. ④ 방법론적으로, 단일 시드 실험의 함정 **4건**(r32 "full FT 동급" = 최량 시드, "key 주입 해로움" = 최악 시드, 확장 학습의 "추월" = 최량 시드, repetition penalty 해악의 과장 = 최악 시드)을 다중 시드로 적발하고, 시드 단위 검정과 예제 단위 McNemar가 서로 다른 질문임("단일-시드 유의성의 함정")을 정리했으며, "용량↑→분산↑" 경향이 과소적합 영역 한정 현상(수렴에서 역전)임을 발견했다. ⑤ Sonnet에서는 재학습 없이 디코딩만으로 chrF **+10.4**(greedy 31.7 → nucleus 샘플링 42.1±0.4, 3-시드 견고)를 얻었고, beam search가 샘플링에 ~10점 뒤지는 음성 결과("우도 최대화 ≠ 생성 품질")를 퇴화 지표(beam의 4-gram 86% 반복 vs rep_penalty의 과교정)로 기계적으로 입증했다. ⑥ 8GB GPU에서 full fine-tuning의 RAM spill(~20배 저하)을 진단해 원인(GPU 사전 점유 + 가변 길이 배치의 라이브 피크 5.4GB+)과 처방(메모리 상한+캐시 회수)을 제시하고, **bf16 혼합정밀이 정확도 손실 없이 최대 +53% 가속**함을 실측했다.

**윤리.** 모든 학습·튜닝·평가는 train/dev만 사용했으며 공식 test 데이터는 사용하지 않았다(상세는 §4.1).

---

## 1. 서론 (Introduction)

### 1.1 연구 배경
GPT-2는 "대규모 비지도 사전학습 후 후속 태스크 미세조정" 패러다임을 정착시킨 디코더 전용 언어 모델이다 [Radford et al., 2019]. 본 프로젝트는 어텐션·트랜스포머 블록·옵티마이저를 직접 구현해 라이브러리 사용과 내부 이해 사이의 격차를 메우고, 동일한 사전학습 가중치가 분류(의역 판별)와 생성(시 창작)이라는 이질적 태스크에 어떻게 전이되는지 실증적으로 탐구한다. 나아가 124M 파라미터 모델조차 전체 미세조정이 부담스러운 **단일 8GB GPU** 환경을 연구 대상으로 삼아, PEFT의 설계 선택과 메모리 제약을 정량 분석한다.

### 1.2 기존 접근의 한계
- **Full fine-tuning의 비용**: 후속 태스크마다 124M 파라미터 전체를 갱신하면 옵티마이저 상태(1·2차 모멘트)까지 포함해 메모리·연산 부담이 크다(§4.7에서 실측).
- **분류 헤드와 사전학습의 불일치**: 무작위 초기화한 2-class 선형 헤드는 GPT-2가 사전학습으로 획득한 어휘 분포 지식을 활용하지 못한다.
- **PEFT 설계 선택의 불투명성**: LoRA를 "어디에(주입 모듈), 얼마나(rank·α)" 적용해야 하는지, ReFT 같은 대안과 어떻게 다른지는 소규모 예산에서 충분히 비교되지 않는다.
- **단일 시드 실험의 신뢰성**: 과제·소규모 연구의 비교 실험은 대부분 단일 시드인데, 그 결론이 학습 난수성에 얼마나 취약한지는 거의 보고되지 않는다.
- **생성 품질의 디코딩 의존성 간과**: 평가지표(chrF)에 대한 디코딩 하이퍼파라미터의 영향은 정량 검증되지 않는 경우가 많다.

### 1.3 본 연구의 기여
1. **Cloze 재구성**: Paraphrase Detection을 "yes"/"no" 토큰 생성 문제로 바꿔 가중치 공유 출력층으로 사전학습 지식을 활용한다(1-epoch dev 86.4%, 3-시드 86.0%±0.8).
2. **PEFT 설계축의 분리**: 동일-step(3,000)·동일-batch 조건에서 모듈·rank·α·dropout·lr 축을 각각 3-시드로 ablation하여 **위계**(모듈 확대: 노이즈성 < rank: +2.4%p 견고 < α 상향: 1/4 파라미터로 r=32 동급)를 규명하고, α×lr 2D sweep으로 α와 lr이 곱(유효 업데이트)으로 환원되지 않는 별개 축임을 보인다. ReFT는 절반 파라미터로 LoRA r8에 일관되게 0.5%p 근접함을 정량화한다.
3. **주장 검증 방법론**: 단일 시드 함정 4건(r32 "동급"·"key 하락"·확장 학습 "추월"·rep_penalty 과장)을 다중 시드 paired 비교로 적발하고, 예제 단위 McNemar(n≈40K)와 시드 단위 검정이 서로 다른 질문임을 정리하며, 1 epoch 완주 수렴 비교로 "PEFT가 충분히 학습하면 따라잡는다" 가설을 기각하고(전 시드 −1.7%p) "용량↑→분산↑"이 과소적합 한정 현상(수렴에서 역전)임을 발견한다. **본 보고서의 모든 헤드라인 비교는 3-시드 검증을 거쳤다.**
4. **디코딩 전략의 chrF ablation**: 샘플링이 greedy 대비 +10.4(3-시드 견고)를 주는 반면 beam search는 샘플링에 ~10점 뒤지고("우도 최대화 ≠ 품질") repetition penalty는 셰익스피어 도메인에서 해로움(음성 결과)을 보고하며, 퇴화 지표(distinct-n·rep-4)로 그 메커니즘을 정량 입증한다.
5. **8GB 제약의 실측과 처방**: full fine-tuning의 RAM spill(~20배 저하)을 진단해 원인이 GPU 사전 점유와 가변 길이 배치의 라이브 피크(5.4GB+)임을 규명하고, 메모리 상한+캐시 회수 레시피로 spill 없는 완주를 달성했으며, bf16 혼합정밀의 무손실 +53% 가속과 PEFT의 메모리 우위를 실측한다.

### 1.4 보고서의 구성
2장 관련 연구, 3장 GPT-2 구현과 확장(Cloze·LoRA·ReFT·디코딩) 및 검증 방법론, 4장 데이터·평가·실험 설정과 정량 결과(기준 모델 비교·의견 포함), 5장 결론·향후 연구로 구성한다.

---

## 2. 관련 연구 (Related Works)

- **GPT-1/GPT-2** [Radford, 2018; Radford et al., 2019]: 단방향 트랜스포머 디코더의 비지도 사전학습이 후속 태스크로 전이됨을 보였다. 본 연구는 사전학습 가중치를 로드해 전이하되, **분류를 생성 형식(Cloze)으로 재구성**한다는 점에서 표준 헤드 부착 방식과 차별된다.
- **Paraphrase Detection** [Fernando & Stevenson, 2008]: 전통적 의미 유사도 접근을 본 연구는 언어모델의 다음 토큰 예측 분포로 환원한다.
- **LoRA** [Hu et al., 2021]: 가중치를 동결하고 저랭크 행렬만 학습하는 PEFT. 원 논문은 대규모 벤치마크에서 "full fine-tuning 동급"을 보고했으나, 본 연구는 **주입 모듈·rank·α·lr를 분리한 ablation**을 소규모·동일-step 조건 + 다중 시드에서 수행해 설계 선택의 효과와 "동급" 주장의 시드 의존성을 검증한다.
- **ReFT(LoReFT)** [Wu et al., 2024]: 가중치가 아닌 **은닉 표현의 저차원 직교 부분공간**에 개입하는 PEFT. 본 연구는 ReFT를 직접 구현하여 LoRA와 동일 조건에서 파라미터 효율·속도·정확도를 직접 비교한다(단순 언급에 그친 기존 과제 보고와 차별).
- **Repetition penalty / CTRL** [Keskar et al., 2019]: 이미 생성된 토큰의 logit을 감쇠해 반복을 억제하는 디코딩 기법. 본 연구는 이것이 chrF에 미치는 영향을 셰익스피어 도메인에서 검증해 **음성 결과**를 얻는다.
- **Neural text degeneration / nucleus 샘플링** [Holtzman et al., 2020]: 우도를 최대화하는 결정론적 디코딩(greedy·beam search)이 반복적·퇴화 텍스트를 낳고 top-p 샘플링이 더 인간다운 생성을 준다는 발견. 본 연구는 beam vs 샘플링의 chrF 격차와 퇴화 지표(distinct-n·rep-4)로 이 현상을 소네트 도메인에서 정량 재현한다.
- **chrF** [Popović, 2015]: 문자 n-gram F-score. 어휘·형태 다양성에 민감하며, 본 연구는 이 특성이 디코딩 전략 선택을 어떻게 좌우하는지 분석한다.

요컨대 본 연구의 차별점은 **단일 8GB GPU·소규모 예산**이라는 현실 제약 아래에서 (i) LoRA 설계축의 분리 ablation, (ii) LoRA vs ReFT 직접 비교, (iii) 메모리 제약의 실측·처방, (iv) 디코딩 전략의 chrF·퇴화 지표 ablation을 **모든 헤드라인 주장에 대한 다중 시드 검증**과 함께 하나의 일관된 실험 틀에서 수행한 점이다.

---

## 3. 제안 방법론 (Proposed Approach)

> **코드 출처 명시**: `models/`, `modules/`, `optimizer.py`, `classifier.py`, `paraphrase_detection.py`, `sonnet_generation.py`의 골격(stub)과 데이터 파이프라인은 과제 제공 starter code이다. §3.1의 누락 코드 블록, §3.2의 Cloze forward, §3.3의 `lora.py`, §3.4의 `reft.py`, §3.5의 `generate()` 확장 및 `generate_beam()`, 그리고 실험 드라이버 일체(`lora_experiments.py`, `multiseed_experiments.py`, `module_multiseed.py`, `lora_hparam_sweep.py`, `alpha_lr_sweep.py`, `stat_analysis.py`, `learning_curve.py`, `multiepoch_experiment.py`, `d8000_multiseed.py`, `amp_experiment.py`, `sonnet_decode_experiment.py`, `sonnet_decode_multiseed.py`, `degeneration_metrics.py`)와 그림 생성(`make_figures.py`)은 **본인이 직접 작성**하였다. LoRA는 Hu et al.(2021), ReFT는 Wu et al.(2024), repetition penalty는 Keskar et al.(2019), 디코딩 퇴화·nucleus 샘플링은 Holtzman et al.(2020), chrF는 Popović(2015)를 참조하였다.

### 3.1 PART-I: GPT-2와 AdamW 구현 (기준 모델)

지정 주제의 기준 모델(GPT-2 124M: vocab 50,257, hidden 768, 12 layers, 12 heads)은 Radford et al.(2019)을 따른다. 직접 구현한 누락 부분은 다음과 같다.

**(a) 인과 자기어텐션** (`modules/attention.py`):
$$
\text{Attention}(Q,K,V)=\text{softmax}\!\left(\frac{QK^\top}{\sqrt{d_k}}+M\right)V,\quad
M_{ij}=\begin{cases}0 & j\le i\\ -\infty & j>i\end{cases}
$$

**(b) pre-LayerNorm 블록** (`modules/gpt2_layer.py`):
$$
h' = h + \text{Drop}(\text{Attn}(\text{LN}_1(h))),\quad
h'' = h' + \text{Drop}(\text{FFN}(\text{LN}_2(h')))
$$

**(c) 임베딩·가중치 공유 출력층** (`models/gpt2.py`): $\text{logits}=h_{\text{final}}\,W_{\text{emb}}^\top$.

**(d) AdamW** (`optimizer.py`): 1·2차 모멘트, 편향 보정, decoupled weight decay.
$$
\hat m_t=\tfrac{m_t}{1-\beta_1^t},\;\hat v_t=\tfrac{v_t}{1-\beta_2^t},\;
\theta_t=\theta_{t-1}-\alpha\!\left(\tfrac{\hat m_t}{\sqrt{\hat v_t}+\epsilon}+\lambda\theta_{t-1}\right)
$$

**(e) 감정 분류기** (`classifier.py`): 마지막 비-패딩 토큰 hidden state에 dropout+선형층. `last-linear-layer`/`full-model` 모드 지원.

검증: `sanity_check.py`가 HuggingFace GPT-2 출력과 `atol=1e-1` 이내 일치, `optimizer_test.py` 통과.

### 3.2 PART-II 확장 ①: Cloze 방식 Paraphrase Detection
평가 코드는 마지막 토큰 logit의 argmax를 **"yes"(BPE 8505)/"no"(BPE 3919)** 와 직접 비교한다. 따라서 `forward()`는 무작위 2-class 헤드 대신 **가중치 공유로 전체 vocab logit**을 반환한다(`paraphrase_detection.py`):
```python
last_token = self.gpt(input_ids, attention_mask)['last_token']
logits = self.gpt.hidden_state_to_token(last_token)   # weight tying → [bs, vocab]
```
무작위 초기화 파라미터가 전혀 없으므로 사전학습된 어휘 분포 지식이 첫 step부터 활용된다.

### 3.3 PART-II 확장 ②: LoRA (configurable PEFT)
$W$를 동결하고 저랭크 보정만 학습한다 [Hu et al., 2021]:
$$
W' = W + \frac{\alpha}{r} BA,\quad A\in\mathbb{R}^{r\times k},\; B\in\mathbb{R}^{d\times r},\; r\ll\min(d,k),\; B=0\ \text{초기화}
$$
직접 작성한 `lora.py`의 `LoRALinear`는 임의 `nn.Linear`를 감싸며, `inject_lora(targets=...)`로 **주입 대상 모듈(query/key/value/attn_out/mlp)과 rank·α·dropout을 선택**할 수 있다 — §4.5의 설계축 ablation은 이 configurability 위에서 수행된다. 구현 중 `from_pretrained`가 일부 층의 `in/out_features` 속성을 stale 상태로 남기는 문제를 발견하여, 차원을 **weight shape에서 직접** 읽도록 수정했다.

### 3.4 PART-II 확장 ③: ReFT (LoReFT) — LoRA 비교군
가중치가 아닌 **은닉 표현 h**에 개입한다 [Wu et al., 2024]. 직접 작성한 `reft.py`는 선택 레이어 출력에 직교 부분공간 개입을 적용한다:
$$
\Phi(h) = h + R^\top\!\left(W h + b - R h\right),\quad R\in\mathbb{R}^{r\times d}\ (\text{직교 행})
$$
$R$의 직교성은 `torch.nn.utils.parametrizations.orthogonal`로 학습 내내 유지하고, 사전학습 가중치는 전부 동결한다.

### 3.5 PART-II 확장 ④: Sonnet 생성과 디코딩
`forward()`는 각 위치의 전체 vocab logit `[bs, seq, vocab]`을 반환(토큰 단위 LM 학습). `generate()`는 temperature·top-k·top-p에 더해 **repetition penalty**(이미 등장한 토큰의 logit 감쇠 [Keskar et al., 2019])를 추가 지원하고, `generate_beam()`은 길이 정규화를 적용한 **결정론적 beam search**를 직접 구현한다(확률적 샘플링과의 비교군).

### 3.6 주장 검증 방법론 (cross-cutting)
비교 실험의 결론이 학습 난수성에 견고한지 확인하기 위해, 다음 4단계 검증 틀을 설계해 모든 헤드라인 주장에 적용했다.
1. **다중 시드 paired 비교**: 핵심 설정을 시드 {11711, 42, 1234}로 반복(시드는 데이터 셔플과 LoRA/ReFT 초기화를 함께 결정). 동일 시드 집합이므로 설정 간 paired Δ(평균차 ± 차이의 표준편차)와 시드별 승패를 보고한다.
2. **예제 단위 McNemar 검정**: 동일 시드에서 두 모델의 dev 40,429개 예제별 정오표를 짝지어 검정(n≈40K로 강력) — 시드 단위(n=3)와 **서로 다른 질문**을 묻는다(§4.5.4).
3. **학습량 차원**: 동일-step(3,000 ≈ 0.17 epoch, 과소적합 영역) 비교와 **1 epoch 완주 수렴** 비교를 분리해, 결론이 학습량에 의존하는지 본다(§4.5.5).
4. **메커니즘 지표**: 생성 품질 주장은 chrF만이 아니라 퇴화 지표(distinct-n, rep-4 [Holtzman et al., 2020])로 원인까지 입증한다(§4.6.3).

---

## 4. 실험 (Experiments)

### 4.1 데이터
| 데이터셋 | 태스크 | 분할 | 비고 |
|---|---|---|---|
| SST [Socher et al., 2013] | 5-class 감정 | train 8,544 / dev 1,101 / test 2,210 | PART-I 검증용 |
| CFIMDB | 2-class 감정 | train 1,701 / dev 245 / test 488 | PART-I 검증용 |
| Quora | Paraphrase(이진) | train 283,003 / dev 40,429 (제공 분할) | Cloze 재구성 |
| Sonnets | 시 생성 | train 143 / held-out 14 | Shakespeare 14행시 |

`datasets.py` 파일 입출력에 `encoding='utf-8'`을 명시해 Windows(cp949) 크래시를 방지했다.

**윤리 준수 (test set 부정 사용 금지)**: 모든 학습·하이퍼파라미터 튜닝·비교 평가는 **train/dev만** 사용했다. 공식 test 데이터는 제출용 예측 파일 생성에만 사용했고 정확도 측정·모델 선택에 일절 사용하지 않았다. Sonnet 디코딩의 chrF는 공식 test가 아니라 **학습 소네트 중 마지막 14편을 자체 held-out**으로 분리해 측정했다.

### 4.2 평가 방법
- Paraphrase: **dev Accuracy**(±시드 표준편차). 보조로 예제 단위 McNemar χ²(§4.5.4).
- Sonnet: **chrF**(sacrebleu) + 퇴화 지표 distinct-1/2(고유 n-gram 비율), rep-4(반복 4-gram 비율).
- 감정 분석(PART-I): 과제 제시 기준값(SST 0.462/0.513, CFIMDB 0.861/0.976) 참조.

### 4.3 실험 세부 정보
- 하드웨어: 단일 **RTX 4060 Ti (8GB)**, Windows, torch 2.5.1+cu121. seed 기본 11711, `cudnn.deterministic=True`.
- 1-epoch 헤드라인(§4.4): batch 16, fp32. full FT lr 1e-5 / LoRA lr 2e-4(r8)·3e-4(r16).
- PEFT ablation(§4.5.1): **batch 8, 동일 3,000 step**(≈0.17 epoch), fp32. full FT lr 1e-5 / LoRA·ReFT lr 2e-4, 확장 설정 D만 8,000 step·lr 3e-4.
- 다중 시드 검증(§4.5.2–4.5.3): 시드 {11711, 42, 1234}, 나머지 설정 동일.
- 통계 검정·학습 곡선(§4.5.4): seed 11711·3,000 step. McNemar는 dev 40,429 전체, 학습 곡선은 dev 2,000 부분집합을 250 step마다 평가.
- 수렴 비교(§4.5.5): full/r32/α64(r8)를 1 epoch 완주(17,688 step, batch 16, bf16)×3시드; D(8,000 step)와 §4.4 r16도 같은 3시드로 승격. run별 자식 프로세스 격리 + 메모리 상한(0.80)·gc_threshold·empty_cache로 8GB spill 방지(§4.7).
- Sonnet(§4.6): 10-epoch 학습 체크포인트 고정, dev 14편의 첫 3행을 조건으로 생성, 단일 시드 sweep 11개 설정 → 대표 4설정 3-시드 + beam {1,3,5}; 퇴화 지표는 동일 생성문에서 측정.
- 혼합정밀(§4.7): full FT를 {fp32, bf16} × {batch 8, 16}으로 각 300 step, `torch.autocast(bfloat16)`, peak VRAM은 `torch.cuda.max_memory_*`.

### 4.4 결과 1 — Paraphrase Detection 헤드라인 (1 epoch, batch 16)

| 설정 | 학습 파라미터 | 비율 | dev acc (seed 11711) | **3-시드 mean ± std** |
|---|---|---|---|---|
| Full fine-tuning (기준 모델) | ~124.4 M | 100% | **0.864** | **0.8600 ± 0.0077** |
| LoRA r=16, α=32, lr 3e-4 (q/v) | 589,824 | 0.47% | 0.853 | 0.8493 ± **0.0033** |
| LoRA r=8, α=16, lr 2e-4 (q/v) | 294,912 | 0.235% | 0.817 | — |

- 충분히 학습(1 epoch)하면 LoRA r=16이 full fine-tuning과 **1.1%p 차**로 근접하며, 이 격차는 3-시드 평균(84.9 vs 86.0)에서도 **정확히 1.1%p로 유지**된다(paired Δ=−0.0107±0.0050, full 3/3승) — 단일 시드 결론이 시드에 견고함을 확인했다.
- full FT의 시드별 값은 0.8643/0.8665/0.8492로, 86.4%는 분포의 대표값이다(자세한 수렴 분석은 §4.5.5).

*예상과의 비교*: LoRA의 근접은 원 논문[Hu et al., 2021]의 보고와 부합해 예상대로였으나, **그 격차(1.1%p)가 0이 아니라 일관되게 유지**된다는 점은 "동급" 통념보다 보수적인 결과다(§4.5.5에서 수렴 영역에서도 따라잡지 못함을 확인).

### 4.5 결과 2 — PEFT 설계축 ablation (★핵심 확장)

#### 4.5.1 동일-step ablation (3,000 step, batch 8, 단일 시드 11711)

공정 비교를 위해 모든 설정을 동일 step·batch로 학습했다(D만 8,000 step).

| 설정 | 주입/방법 | 학습 파라미터 | 비율 | it/s | dev acc |
|---|---|---|---|---|---|
| **D: LoRA all-modules r=16 (8,000 step, lr 3e-4)** | Q/K/V+attn_out+MLP | 2,654,208 | 2.079% | 25.9 | **0.8186** |
| **B: LoRA r=32 (q/v)** | query/value | 1,179,648 | 0.935% | 32.9 | **0.8011** |
| A: **full fine-tuning** (참조) | 전체 | 125,030,400 | 100% | 14.7 | 0.8001 |
| A: LoRA r=8 (q/v) | query/value | 294,912 | 0.235% | 32.3 | 0.7784 |
| A: LoRA r=8 (q/k/v+out) | +attn_out | 589,824 | 0.470% | 30.3 | 0.7764 |
| C: **ReFT r=8** | 표현 개입(전층) | 147,552 | 0.118% | 18.5 | 0.7735 |
| A: LoRA r=8 (all-modules) | Q/K/V+out+MLP | 1,327,104 | 1.050% | 26.0 | 0.7720 |
| B: LoRA r=16 (q/v) | query/value | 589,824 | 0.470% | 33.1 | 0.7691 |
| C: ReFT r=4 | 표현 개입(전층) | 73,776 | 0.059% | 23.9 | 0.7684 |
| B: LoRA r=4 (q/v) | query/value | 147,456 | 0.118% | 32.9 | 0.7580 |
| A: LoRA r=8 (q/k/v) | +key | 442,368 | 0.353% | 31.3 | 0.7521 |

단일 시드에서 보이는 인상: ① r=32 ≈ full FT(80.1 vs 80.0), ② 확장 학습 D가 full FT 추월(81.9), ③ key 주입이 오히려 해로움(75.2), ④ rank 확대(r4→r32: 75.8→80.1)가 모듈 확대보다 큼, ⑤ ReFT는 절반 파라미터로 LoRA r8에 근접. **이 인상들이 시드에 견고한지를 §4.5.2–4.5.5에서 검증하며, ①②③은 함정으로 판명된다.**

> §4.4(1 epoch)와 §4.5(3,000 step)는 학습량·batch가 달라 절대 수치를 직접 비교할 수 없다. §4.5는 설계 선택을 분리하기 위한 동일-step 비교, §4.4는 수렴 성능이다.

#### 4.5.2 다중 시드 재검증 — rank·모듈·ReFT (3 시드, paired)

**(1) rank·ReFT** (`multiseed_experiments.py`)

| 설정 | 비율% | it/s | **mean ± std** | min~max | 시드별 (11711/42/1234) |
|---|---|---|---|---|---|
| full fine-tuning | 100 | 15.3 | **0.8019 ± 0.0013** | 0.8001~0.8031 | 0.8001 / 0.8031 / 0.8026 |
| LoRA r=32 (q/v) | 0.935 | 32.5 | 0.7887 ± **0.0145** | 0.7684~0.8011 | 0.8011 / 0.7966 / 0.7684 |
| LoRA r=8 (q/v) | 0.235 | 32.9 | 0.7769 ± 0.0011 | 0.7761~0.7784 | 0.7784 / 0.7761 / 0.7762 |
| LoRA r=16 (q/v) | 0.470 | 32.8 | 0.7768 ± 0.0091 | 0.7691~0.7896 | 0.7691 / 0.7896 / 0.7717 |
| ReFT r=8 | 0.118 | 18.5 | 0.7717 ± 0.0038 | 0.7664~0.7753 | 0.7735 / 0.7664 / 0.7753 |
| LoRA r=4 (q/v) | 0.118 | 33.0 | 0.7647 ± 0.0047 | 0.7580~0.7684 | 0.7580 / 0.7684 / 0.7677 |

| 주장 | 비교 | Δ(mean) | 승패 | 판정 |
|---|---|---|---|---|
| ① r32 ≈ full FT? | r32 − full | −0.0133 ± 0.0152 | full **2/3 승** | **함정**: r32의 80.1%는 *최량 시드*, 평균 1.3%p 열위 |
| ④ rank 효과 | r32 − r4 | +0.0239 ± 0.0176 | r32 **3/3 승** | rank 확대 양극단에서 견고 |
| ④' mid-rank | r16 − r8 | −0.0001 ± 0.0098 | 1/3 (분할) | **노이즈 확인** |
| ⑤ ReFT 근접 | r8 − reft | +0.0052 ± 0.0036 | r8 **3/3 승** | ReFT 절반 파라미터로 일관되게 0.5%p 근접 |

**(2) 모듈** (LoRA r=8 고정, 주입 모듈만 누적, `module_multiseed.py`)

| 주입 모듈 | 비율% | **mean ± std** | 시드별 (11711/42/1234) | q/v 대비 paired |
|---|---|---|---|---|
| q/v | 0.235 | 0.7769 ± **0.0011** | 0.7784 / 0.7761 / 0.7762 | — |
| +key (q/k/v) | 0.353 | 0.7673 ± 0.0123 | 0.7521 / 0.7824 / 0.7673 | −0.0096 ± 0.0133 (1/3, **노이즈**) |
| +attn_out | 0.470 | 0.7785 ± 0.0079 | 0.7764 / 0.7890 / 0.7701 | +0.0016 ± 0.0082 (노이즈) |
| all (+mlp) | 1.050 | **0.7872** ± 0.0121 | 0.7720 / 0.7880 / 0.8017 | +0.0103 ± 0.0131 (2/3, 경계) |

![PEFT 효율-정확도](figures/fig_peft_efficiency.png)

*그림 1 — 동일 예산(3,000 step)에서 학습 파라미터 비율 vs dev 정확도(3-시드 mean±std). LoRA rank 사다리(파랑), ReFT(보라), α=64/r=8(★, §4.5.3)을 full FT(빨강 점선±음영)와 비교. r=32의 큰 오차막대(시드 분산)와, α=64/r=8이 1/4 파라미터로 r=32와 동급임이 보인다.*

**정량적 해석**
- **주장 수정 ①**: r32는 full FT와 "동급"이 아니다 — 단일 시드의 80.1%는 r32 분포의 최댓값(min 76.8%)이었다. **PEFT의 실질 이점은 정확도 동급이 아니라 99% 적은 파라미터·2배 속도라는 효율**이다.
- **주장 수정 ③**: "key 추가가 해롭다"도 단일 시드 함정 — §4.5.1의 q/k/v 75.2%는 분포의 *최악 시드*였고(다른 시드 78.2%), 평균은 q/v와 노이즈 내에서 겹친다.
- **위계 정리**: rank 확대(r4→r32, +2.4%p, 3/3 견고) > 모듈 확대(q/v→all, +1.0%p, 2/3, 노이즈성). 둘 다 용량을 늘리지만 rank 쪽이 신뢰할 수 있는 지렛대다. mid-rank(r8↔r16) 차이는 노이즈로 확증.
- **분산이 용량에 비례**: full FT(std 0.0013)·저랭크(0.001~0.005)는 안정적인 반면 고랭크 r16/r32(0.009~0.015)는 시드 민감 — 단일 시드로는 보이지 않던 현상이다(이 경향의 적용 한계는 §4.5.5에서 드러난다).

*예상과의 비교*: rank의 효과는 예상대로였으나, **r32 "동급"이 시드 운이라는 점과 용량↑→분산↑ 현상은 예상 밖**이었다. 짧은 예산에서 고용량 어댑터는 충분히 수렴하지 못해 초기화·셔플 난수에 민감해진다고 해석한다.

#### 4.5.3 α(scaling)·dropout·lr — 더 값싼 지렛대의 발견

LoRA 업데이트는 $(\alpha/r)BA$이므로 α는 LoRA 경로의 유효 업데이트 크기를 조절한다. rank(r=8)·모듈(q/v)·step(3,000)을 고정하고 α, dropout, lr을 분리 sweep했다(`lora_hparam_sweep.py`, `alpha_lr_sweep.py`, 각 3-시드).

**(1) α sweep** (dropout=0, lr 2e-4) — α 8→64에서 76.6→**78.9%** 단조·견고 상승(α=32, 64는 기준 α=16 대비 **3/3 시드 우세**).

| α (α/r) | 8 (×1) | **16 (×2)** 기준 | 32 (×4) | 64 (×8) |
|---|---|---|---|---|
| mean ± std | 0.7659±0.0025 | 0.7769±0.0011 | 0.7854±0.0027 | **0.7890±0.0051** |

- **🔑 핵심 발견**: α=64/r=8은 **0.235% 파라미터**로 78.90% — §4.5.2의 r=32(0.935%, 78.87%)와 **사실상 동일**하다. 즉 "rank 확대"로 귀속됐던 향상의 상당 부분은 **유효 업데이트 크기(α/r) 효과**이며, α는 파라미터를 늘리지 않는 더 값싼 지렛대다.

**(2) dropout sweep** (α=16) — 0→0.2에서 0.7769→0.7727로 미세 단조 하락(대부분 노이즈). 3,000 step은 283K Quora를 한 번도 다 못 보는 **과소적합 영역**이라 정규화할 과적합이 없고 dropout은 신호만 깎는다.

**(3) α × lr 2D sweep** — "α = 유효 lr"이라면 α를 키우는 것과 lr을 키우는 것이 동치인가?

| α \ lr | 1e-4 | 2e-4 | 4e-4 |
|---|---|---|---|
| **α=16** | 0.7501 ± 0.0027 | 0.7769 ± 0.0011 | 0.7863 ± 0.0026 |
| **α=64** | 0.7795 ± 0.0022 | 0.7890 ± 0.0051 | **0.7940 ± 0.0075** |

![α×lr 히트맵](figures/fig_alpha_lr_heatmap.png)

*그림 2 — α×lr 격자의 dev 정확도(3-시드). 대각 방향(우하)으로 단조 상승하나, iso-product 두 칸(α16/lr4e-4 vs α64/lr1e-4)이 다른 값을 가져 α와 lr이 곱으로 환원되지 않음을 보인다.*

- **α와 lr은 별개 축**: 유효 업데이트(∝ α/r·lr)가 같은 두 점(8e-4)에서 α=16/lr=4e-4(0.7863)가 α=64/lr=1e-4(0.7795)보다 높다(Δ=−0.0068±0.0006, 매우 견고). Adam이 그래디언트를 좌표별로 정규화하므로 lr(스텝 크기)과 α(forward 출력·그래디언트 스케일)는 학습 동역학에서 다르게 작용한다.
- **둘 다 키우는 것이 최선**: 격자 최고 α=64/lr=4e-4 = **79.40%**는 r=32를 **1/4 파라미터로 추월**한다. 단 공격적일수록 분산이 커진다(std 0.0075).

*예상과의 비교*: α의 효과 방향은 예상대로였으나 **그 크기(rank를 대체할 정도)는 예상 이상**이었고, iso-product가 깨진 것(α≠lr)은 Adam의 정규화를 고려하면 사후적으로 설명되나 사전에는 동치라고 예상했었다.

#### 4.5.4 예제 단위 McNemar 검정과 학습 곡선

**(A) McNemar** (`stat_analysis.py`, seed 11711, dev n=40,429) — 시드 단위(n=3)는 검정력이 약하므로 예제 단위로 보강한다.

| 비교 (A vs B) | A만 맞음 | B만 맞음 | χ² | p-value | 판정 |
|---|---|---|---|---|---|
| r32 vs full | 1,973 | 1,934 | 0.37 | 0.54 | **유의X (동급)** |
| r16 vs r8 | 3,512 | 3,889 | 19.10 | 1.2e-5 | 유의, r8↑ |
| alpha64 vs r32 | 2,005 | 2,235 | 12.37 | 4.4e-4 | 유의, r32↑ |
| alpha64 vs full | 2,486 | 2,677 | 6.99 | 8.2e-3 | 유의, full↑ |

- **r32 ≈ full을 예제 단위로도 확증**: r32의 *최량 시드*에서조차 full과의 차이가 유의하지 않다(p=0.54).
- **⚠️ 단일-시드 유의성의 함정 (방법론적 발견)**: r16 vs r8은 예제 단위로는 r8이 유의하게 우세(p=1.2e-5)지만 시드 단위로는 노이즈(§4.5.2)였다. 예제 단위 McNemar는 "이 학습된 쌍의 예측 차이가 우연인가", 다중 시드는 "결론이 학습 난수성에 견고한가"를 묻는 **서로 다른 질문**이며, 설계 권고는 두 스코프 모두에서 살아남는 결론에만 근거해야 한다.

**(B) 학습 곡선** (`learning_curve.py`, dev 2,000 부분집합)

![학습 곡선](figures/fig_learning_curve.png)

*그림 3 — 3,000-step 예산의 학습 곡선(dev 2k, seed 11711). LoRA 계열의 빠른 초기 적응과 세 설정의 공통 수렴이 보인다.*

- 초반(≤750 step)엔 변동이 크지만 LoRA(r32·alpha64)가 full FT보다 빠르게 적정 정확도에 도달하는 경향(step 500: ~0.68 vs 0.65)을 보이고, 세 설정 모두 step ~2,500에서 ~0.80으로 수렴한다. 학습은 발산·정체 없이 효과적으로 진행됐다. 곡선은 단일 시드·부분집합이라 정성 관찰로 보며, "공통 수렴"이 더 긴 학습에서도 유지되는지는 §4.5.5가 답한다.

#### 4.5.5 수렴 비교 — PEFT는 충분히 학습하면 따라잡는가 (★)

3,000 step은 과소적합 영역이었다. full FT / LoRA r=32 / LoRA α=64(r=8)를 **1 epoch 완주**(17,688 step, batch 16, bf16)·3시드로 비교하고(`multiepoch_experiment.py`), 단일 시드로 남아 있던 확장 학습 D와 §4.4 r16도 같은 시드로 승격했다(`d8000_multiseed.py`, `paraphrase_detection.py --seed`).

| 설정 | 비율% | it/s | **mean ± std** | 시드별 (11711/42/1234) | full 대비 paired Δ |
|---|---|---|---|---|---|
| full fine-tuning (1 epoch) | 100 | 11.3 | **0.8600 ± 0.0077** | 0.8643 / 0.8665 / 0.8492 | — |
| LoRA r=16, lr 3e-4 (1 epoch, §4.4) | 0.470 | — | 0.8493 ± 0.0033 | 0.853 / 0.850 / 0.845 | −0.0107 ± 0.0050 (full **3/3승**) |
| LoRA r=32 (1 epoch) | 0.935 | 24.2 | 0.8430 ± **0.0015** | 0.8418 / 0.8420 / 0.8450 | −0.0170 ± 0.0091 (full **3/3승**) |
| LoRA α=64, r=8 (1 epoch) | 0.235 | 24.2 | 0.8379 ± 0.0053 | 0.8338 / 0.8345 / 0.8454 | −0.0221 ± 0.0130 (full **3/3승**) |
| D: all-modules r16 (8,000 step, batch 8) | 2.079 | 25.9 | 0.8057 ± **0.0129** | 0.8186 / 0.7881 / 0.8103 | vs full 3,000-step: +0.0037 ± 0.0140 (**2/3승, 노이즈**) |

![수렴 비교](figures/fig_convergence.png)

*그림 4 — 동일-step(3,000)과 1 epoch 완주의 dev 정확도(3-시드 mean±std). full FT의 우위가 수렴에서도 유지되고, 3,000 step에서 r32와 동급이던 α64/r8이 수렴에선 뒤지며, 오차막대의 역전(r32: 큼→작음, full: 작음→큼)이 보인다.*

**정량적 해석**
- **"따라잡는다" 가설 기각**: 1 epoch 완주에서 full FT(86.0%)가 r32(84.3%)를 전 시드에서 1.7%p 앞선다 — 동일-step의 1.3%p 열위가 수렴에서 좁혀지기는커녕 약간 벌어졌다. PEFT의 가치는 역시 **효율**(0.94% 파라미터, 2.1배 속도: 12분 vs 26분/epoch)이다.
- **주장 수정 ② — D의 "추월"은 네 번째 단일 시드 함정**: §4.5.1의 81.86%는 D 분포의 최량 시드였다. 3-시드 평균(80.6%±1.3)은 full FT 동일-step(80.2%±0.1)과 **노이즈 내 동급**이고 최악 시드(78.8%)는 full FT보다 낮다. 공격적 설정(all-modules + lr 3e-4)의 고분산(std 0.0129)은 "강도↑→분산↑" 패턴 그대로다. 수정된 결론: "싸게 더 오래"는 full FT 동일-step을 *평균적으로 따라잡는* 수준이며, 추월은 시드 운이다.
- **🔑 분산 역전 — '용량↑→분산↑'은 과소적합 영역 한정**: 3,000 step에서 r32는 고분산(std 0.0145)·full FT는 안정(0.0013)이었는데 1 epoch에서는 **정확히 반대**다(r32 0.0015 vs full 0.0077). 고용량 어댑터의 시드 민감성은 수렴하면서 사라진다.
- **α/r 효과는 초기 적응 가속에 국한**: 3,000 step에서 r32와 동급이던 α=64/r=8이 1 epoch에서는 r32에 평균 0.5%p 뒤진다(2/3 시드). 유효 업데이트 크기는 *빨리 적응하게* 해줄 뿐, 수렴 정확도의 상한은 실제 표현 용량(rank)이 결정한다.
- **lr > rank의 1-epoch 재현**: r16@lr3e-4(0.470%, 84.9%)가 r32@lr2e-4(0.935%, 84.3%)보다 높다 — 파라미터 절반으로 0.6%p 우위(§4.5.3과 일관).
- **부수 관찰(lr 민감성)**: D를 lr 2e-4로 학습하면 같은 시드(11711)에서 82.96%(원 lr 3e-4: 81.86%)가 나왔다(`d8000_lr2e4.log`) — 8,000-step all-modules 영역에서는 lr 3e-4가 과도해 분산을 키우고 평균을 깎았을 가능성이 있는 단일 관찰이다.
- **§4.4 헤드라인의 대표성**: full FT 86.4%(fp32)는 3-시드 분포(84.9~86.7%)의 대표값이고, bf16 동일 시드가 86.43%로 재현돼 §4.7의 "bf16 무손실"이 전체 epoch 규모에서 재확인됐다.

*예상과의 비교*: "충분히 학습하면 PEFT가 따라잡는다"는 통념(원 논문의 대규모 결과)에 기대어 격차 축소를 예상했으나 **유지·확대가 관측**됐다 — 124M의 작은 모델에서는 어댑터 용량이 실제 병목이 된다고 해석한다. 분산 역전 역시 예상 밖의 발견으로, "고용량=고분산"이라는 §4.5.2의 일반화가 학습량 조건부임을 가르쳐 준다.

#### 4.5.6 종합 — 살아남은 주장과 강등된 주장

| 단일 시드 주장 (§4.5.1) | 다중 시드 판정 | 근거 |
|---|---|---|
| r32 ≈ full FT "동급" | ❌ **강등** (최량 시드 효과, 평균 −1.3%p) | §4.5.2 |
| 확장 학습 D가 full FT "추월" | ❌ **강등** (최량 시드 효과, 노이즈 내 동급) | §4.5.5 |
| key 주입이 "해로움" | ❌ **강등** (최악 시드 효과, 노이즈) | §4.5.2 |
| rank 확대 효과 (r4→r32) | ✅ 견고 (3/3) | §4.5.2 |
| ReFT의 절반-파라미터 근접 | ✅ 견고 (3/3) | §4.5.2 |
| §4.4 r16 "1.1%p 근접" | ✅ 견고 (격차 정확히 유지) | §4.5.5 |
| α=64/r8 ≈ r32 (1/4 파라미터) | ✅ 견고 (동일-step에서; 수렴에선 r32 우세) | §4.5.3, §4.5.5 |

단일 시드 비교의 주요 주장 중 3건이 뒤집혔다(생성 쪽 rep_penalty 과장까지 포함하면 4건, §4.6.2). 이는 소규모 예산의 비교 실험에서 **다중 시드 검증이 선택이 아니라 필수**임을 보여 준다.

### 4.6 결과 3 — Sonnet 디코딩 전략

#### 4.6.1 단일 시드 sweep (dev chrF, seed 11711)

| 순위 | 전략 | chrF |
|---|---|---|
| 1 | **temperature=1.2 + top-p=0.9** (기본값) | **42.56** |
| 2 | top-p=0.9, temp=0.9 | 41.08 |
| 3 | temperature=1.0 | 40.83 |
| 4 | temperature=0.7 / top-k=40(temp0.9) | 39.98 |
| 6 | top-k=40 + top-p=0.9 | 39.88 |
| 7 | top-p0.9 t1.0 + rep_penalty=1.2 | 39.06 |
| 8 | best + rep_penalty=1.3 | 38.82 |
| 9 | top-p=0.9, temp=0.7 | 38.73 |
| 10 | best + rep_penalty=1.2 | 38.00 |
| 11 | **greedy** | **31.68** |

- **greedy → 최적 샘플링 = +10.9 chrF**를 학습 비용 0으로 달성. temperature를 0.7로 낮추면 오히려 하락 — chrF가 보상하는 어휘 다양성을 보수적 디코딩이 깎는다.

#### 4.6.2 다중 시드 + beam search (`sonnet_decode_multiseed.py`)

| 전략 | 종류 | **chrF mean ± std** | 시드별 |
|---|---|---|---|
| temp=1.2 + top-p=0.9 (기본값) | 샘플링 | **42.06 ± 0.36** | 42.56 / 41.81 / 41.79 |
| temp=1.0 | 샘플링 | 41.29 ± 0.34 | 40.83 / 41.64 / 41.40 |
| top-p=0.9, temp=0.9 | 샘플링 | 41.12 ± 0.09 | 41.08 / 41.25 / 41.03 |
| best + rep_penalty=1.2 | 샘플링 | 39.79 ± **1.30** | 38.00 / 40.36 / 41.02 |
| **beam=3** | 결정론적 | 32.19 | (시드 무관) |
| beam=5 | 결정론적 | 31.86 | (시드 무관) |
| beam=1 (=greedy) | 결정론적 | 31.70 | (시드 무관) |

- **greedy→샘플링 +10.4 견고**: 최적 설정의 *최저* 시드(41.79)조차 모든 beam·greedy보다 +9.6 위.
- **beam search의 음성 결과**: beam=3은 greedy 대비 +0.5에 그치고 beam=5는 하락, 모든 beam이 샘플링에 ~10점 뒤진다 — **"우도 최대화 ≠ 생성 품질"** [Holtzman et al., 2020]의 정량 재현.
- **rep_penalty 음성 결과 — 진짜이나 단일 시드가 과장**: 단일 시드의 38.00은 *최악 시드*였고 3-시드 평균은 39.79±1.30(best 대비 −2.3; 단일 시드로는 −4.6로 과장). 여전히 음성이며 분산이 가장 크다.
- **기본값의 견고성**: temp1.2+top-p0.9는 평균 최고이자 저분산 — 제공 기본값이 잘 설정되어 있음을 다중 시드로 검증.

#### 4.6.3 퇴화 지표 — chrF 격차의 메커니즘 (`degeneration_metrics.py`)

| 전략 | chrF | distinct-1 | distinct-2 | rep-4 | mean_len |
|---|---|---|---|---|---|
| best: temp1.2+top-p0.9 | 42.1 | 0.754 | 0.945 | **0.000** | 124.4 |
| rep_penalty=1.2 | 39.8 | **0.998** | 1.000 | 0.000 | 85.8 |
| beam=3 | 32.2 | **0.100** | 0.119 | **0.861** | 128.0 |
| greedy | 31.7 | 0.123 | 0.162 | 0.798 | 122.8 |

![디코딩 chrF와 퇴화 지표](figures/fig_sonnet_chrf.png)

*그림 5 — (좌) 디코딩 전략별 chrF(3-시드 mean±std; 결정론적 전략은 시드 무관). (우) 퇴화 지표: beam·greedy의 낮은 distinct-1·높은 rep-4(반복 퇴화)와 rep_penalty의 distinct-1≈1(과교정). 점선은 최적 샘플링의 distinct-1 수준.*

- **beam·greedy의 퇴화가 수치로 확정**: beam=3은 고유 unigram이 10%뿐, 4-gram의 86%가 반복 — 같은 구절을 되풀이하는 전형적 퇴화로 chrF 31~32에 머문다.
- **rep_penalty는 '과교정'**: distinct-1을 0.998까지 끌어올리고 길이를 124→86으로 줄여, 반대 방향으로 원본 분포에서 멀어진다. **다양성은 많을수록 좋은 게 아니라 원본 수준에 맞아야 하며**, chrF 순위(42.1>39.8>32.2>31.7)는 원본 다양성과의 일치도를 그대로 반영한다.

*예상과의 비교*: 샘플링의 우위는 예상대로였으나, **repetition penalty가 해롭다는 것은 예상 밖**이었다 — 셰익스피어 소네트의 의도적 반복·대구(parallelism)와 반복 억제가 충돌하기 때문으로, 일반 텍스트의 통념이 도메인에 따라 뒤집힘을 보여 준다. temperature 하향의 역효과도 (정확도 지표 직관과 달리) chrF의 다양성 민감성 때문으로 사후 설명된다.

### 4.7 결과 4 — 현실적 제약: 8GB VRAM, RAM spill, 혼합정밀

**(1) spill 진단.** full fine-tuning(batch 16) 중 **GPU 사용률 100%인데 전력은 160W 중 ~40W**, 학습이 정상 대비 **약 20배 느려지는** 현상을 관측했다. 당시 VRAM은 7.84/8.19GB 점유(여유 110MB)였고, 요구 메모리가 잔여 공간을 넘자 Windows(WDDM) 드라이버가 부족분을 **시스템 RAM으로 spill**한 것이 원인이다. batch 8로 낮추면 정상 복귀했다.

**(2) 혼합정밀 측정** (`amp_experiment.py`, 빈 GPU, 300 step):

| precision | batch | it/s | peak alloc | peak reserved | dev acc (300 step) |
|---|---|---|---|---|---|
| fp32 | 8 | 14.72 | 2.55 GB | 3.09 GB | 0.6763 |
| fp32 | 16 | 9.43 | 3.43 GB | 3.86 GB | 0.5777 |
| bf16 | 8 | 18.25 | 2.52 GB | 3.03 GB | 0.6770 |
| bf16 | 16 | 14.41 | 3.09 GB | 3.46 GB | 0.5776 |

- **spill은 점유 문제**: 빈 GPU에서 batch 16의 peak reserved는 3.86GB로 본질적으로 8GB를 넘지 않는다 — §(1)의 20배 저하는 관측 시점에 다른 프로세스가 ~7.7GB를 점유했기 때문이다.
- **bf16**: batch 8 +24%, batch 16 **+53%** 가속, **정확도는 동일 batch에서 fp32와 사실상 같다**(0.6770 vs 0.6763 등) — bf16 batch16 ≈ fp32 batch8 속도로, 같은 속도에 배치를 2배 키울 수 있다. 절감은 활성값에 국한(파라미터·옵티마이저는 fp32 유지).
- **(한정) 전체 epoch의 피크는 더 크다**: §4.5.5 실험 중, 가변 길이 Quora 배치의 초장문 배치에서 라이브 피크가 **5.4GB+**까지 튀는 것을 실측했다(0.72×8GB 상한에서 OOM 발생; 300-step 측정은 이를 못 본다). 다른 프로세스가 ~1.2GB를 점유한 GPU에서 spill 없이 완주하려면 **per-process 메모리 상한(0.80) + `garbage_collection_threshold` + 주기적 `empty_cache`** 조합이 유효했다(학습 수치 불변, full FT 11.3 it/s로 소폭 오버헤드).

**(3) PEFT의 메모리 우위.** LoRA/ReFT는 사전학습 가중치를 동결해 옵티마이저 상태가 학습 파라미터(0.06~2%)에만 필요하므로, 점유된 환경에서도 여유롭게 적재되어 30 it/s 이상으로 학습된다.

*예상과의 비교*: full FT가 8GB를 본질적으로 초과해 spill이 났으리라 예상했으나, 측정 결과 **원인은 외부 점유**였고 본질 요구는 ~3.9GB(300 step 기준)였다 — 다만 전체 epoch의 간헐적 피크(5.4GB+)는 그 측정조차 과소추정임을 보여, **실험 재현 보고에는 batch 크기뿐 아니라 GPU 사전 점유 상태와 시퀀스 길이 분포까지 필요**하다는 교훈을 얻었다.

---

## 5. 결론 및 향후 연구 (Conclusion & Future Work)

### 5.1 요약
1. **구현**: GPT-2(124M)와 AdamW를 바닥부터 완성해 `sanity_check`(atol 1e-1)를 통과했다(PART-I).
2. **Cloze Paraphrase Detection**: 1-epoch dev 86.4%(3-시드 86.0%±0.8)의 기준 모델을 확보했고, LoRA r16은 1.1%p 차로 근접(3-시드 확증)했다.
3. **PEFT 설계축**: 동일-step·다중 시드 ablation으로 위계를 분리했다 — 모듈 확대(노이즈성) < rank(+2.4%p 견고) < **α 상향(1/4 파라미터로 r32 동급)**; α와 lr은 별개 축(격자 최고 79.4%가 r32 추월); ReFT는 절반 파라미터로 LoRA r8에 일관 근접; dropout은 과소적합 예산에서 무익. 1 epoch 수렴 비교는 **"PEFT가 충분히 학습하면 따라잡는다" 가설을 기각**했고(전 시드 −1.7%p; 단 LoRA는 2.1배 빠름), α/r 효과가 초기 적응 가속에 국한됨과 "용량↑→분산↑"의 수렴 시 역전을 발견했다.
4. **검증 방법론**: 단일 시드 함정 4건(r32 "동급", D "추월", "key 해로움", rep_penalty 과장)을 적발했고, 시드 단위와 예제 단위(McNemar) 유의성이 서로 다른 질문임을 정리했다. 본 보고서의 모든 헤드라인 비교는 3-시드 검증을 거쳤다.
5. **Sonnet 디코딩**: 재학습 없이 chrF +10.4(견고). beam search는 샘플링에 ~10점 뒤지고("우도≠품질") repetition penalty는 이 도메인에서 해로우며, 퇴화 지표로 메커니즘(beam의 rep-4 86% vs rep_penalty의 distinct-1 0.998 과교정)을 입증했다.
6. **8GB 제약**: RAM spill(~20배 저하)의 원인(외부 점유+라이브 피크 5.4GB+)과 처방(메모리 상한+캐시 회수)을 실측으로 제시했고, bf16이 무손실 +53% 가속임을 확인했다.

### 5.2 한계
- 시드 단위 검정은 n=3이라 검정력이 약하다(예제 단위 McNemar는 단일 시드 내에서만 유효). 시드 수 확대 후 교차-시드 형식 검정은 향후 과제다.
- §4.4·§4.5의 모든 헤드라인 설정은 3-시드 승격을 마쳤으나, 디코딩 chrF의 held-out이 14편이라 절대 수치의 표본은 여전히 작다.
- 감정 분석의 팀 자체 측정값은 본 보고서에 포함하지 않고 과제 제시 기준값(SST 0.462/0.513, CFIMDB 0.861/0.976)을 참조했다.
- D의 lr 민감성 관찰(§4.5.5)은 단일 시드 1건이라 추가 검증이 필요하다.

### 5.3 향후 연구
- 1 epoch을 넘는 다중 에폭(dropout이 의미를 갖는 과적합 영역)에서 full FT vs PEFT의 과적합 내성 비교; 더 큰 rank 영역과 α·lr 동시 상향의 안정성 경계 탐색.
- ReFT 개입 위치(특정 토큰·일부 레이어) 최적화, prefix/prompt-tuning 등 다른 PEFT와 추가 비교.
- Sonnet: 운율(rhyme) 제약 디코딩, held-out 확대.
- fp16+GradScaler·gradient checkpointing으로 더 큰 배치/모델 적재 검토.

---

## 6. 참고 문헌 (References)
1. R. Socher et al. *Recursive deep models for semantic compositionality over a sentiment treebank.* EMNLP 2013.
2. S. Fernando, M. Stevenson. *A semantic similarity approach to paraphrase detection.* CLUK 2008.
3. A. Radford. *Improving language understanding by generative pre-training.* 2018.
4. A. Radford et al. *Language models are unsupervised multitask learners.* OpenAI blog, 2019.
5. M. Popović. *chrF: character n-gram F-score for automatic MT evaluation.* WMT 2015.
6. E. J. Hu et al. *LoRA: Low-Rank Adaptation of Large Language Models.* 2021.
7. Z. Wu et al. *ReFT: Representation finetuning for language models.* 2024.
8. N. S. Keskar et al. *CTRL: A Conditional Transformer Language Model for Controllable Generation.* 2019.
9. A. Holtzman et al. *The Curious Case of Neural Text Degeneration.* ICLR 2020.

---

## 부록 A. 재현 명령어
```bash
# PART-I
python sanity_check.py
python optimizer_test.py
python classifier.py --fine-tune-mode last-linear-layer --use_gpu
python classifier.py --fine-tune-mode full-model --use_gpu

# PART-II Paraphrase — 헤드라인(1 epoch, §4.4)
python paraphrase_detection.py --use_gpu --epochs 1 --batch_size 16            # full FT
python paraphrase_detection.py --use_gpu --epochs 1 --batch_size 16 --lr 3e-4 \
    --use_lora --lora_rank 16 --lora_alpha 32                                  # LoRA r16

# PART-II Paraphrase — PEFT ablation (§4.5.1, 단일 시드)
python lora_experiments.py --use_gpu --batch_size 8 --max_steps 3000 --full_steps 8000
# 다중 시드 재검증: rank·ReFT / 모듈 / α·dropout / α×lr (§4.5.2–4.5.3)
python multiseed_experiments.py --use_gpu --batch_size 8 --max_steps 3000 --seeds 11711 42 1234
python module_multiseed.py --use_gpu --batch_size 8 --max_steps 3000 --seeds 11711 42 1234
python lora_hparam_sweep.py --use_gpu --batch_size 8 --max_steps 3000 --seeds 11711 42 1234
python alpha_lr_sweep.py --use_gpu --batch_size 8 --max_steps 3000 --seeds 11711 42 1234
# McNemar 검정 + 학습 곡선 (§4.5.4)
python stat_analysis.py --use_gpu --batch_size 8 --max_steps 3000
python learning_curve.py --use_gpu --batch_size 8 --max_steps 3000 --eval_every 250 --dev_subset 2000
# 수렴 비교(1 epoch) + D + §4.4 r16 시드 승격 (§4.5.5)
python multiepoch_experiment.py --use_gpu --batch_size 16 --epochs 1 --seeds 11711 42 1234
python d8000_multiseed.py --use_gpu --batch_size 8 --max_steps 8000 --seeds 11711 42 1234
python paraphrase_detection.py --use_gpu --epochs 1 --batch_size 16 --lr 3e-4 \
    --use_lora --lora_rank 16 --lora_alpha 32 --seed 42 \
    --para_dev_out predictions/para-dev-lora16-s42.csv --para_test_out predictions/para-test-lora16-s42.csv
# 혼합정밀(bf16) 메모리·속도 측정 (§4.7)
python amp_experiment.py --use_gpu --steps 300

# PART-II Sonnet — 학습 + 제출 파일
python sonnet_generation.py --use_gpu --epochs 10 --batch_size 8
# 디코딩 sweep(단일 시드, §4.6.1) / 다중 시드+beam(§4.6.2) / 퇴화 지표(§4.6.3)
python sonnet_decode_experiment.py --use_gpu
python sonnet_decode_multiseed.py --use_gpu --seeds 11711 42 1234
python degeneration_metrics.py --use_gpu

# 그림 재생성 (부록 B)
python make_figures.py
```

## 부록 B. 산출물
- **코드(직접 작성)**: `lora.py`, `reft.py`, `lora_experiments.py`, `multiseed_experiments.py`, `module_multiseed.py`, `lora_hparam_sweep.py`, `alpha_lr_sweep.py`, `stat_analysis.py`, `learning_curve.py`, `multiepoch_experiment.py`, `d8000_multiseed.py`, `amp_experiment.py`, `sonnet_decode_experiment.py`, `sonnet_decode_multiseed.py`, `degeneration_metrics.py`, `make_figures.py`; 수정된 `paraphrase_detection.py`·`sonnet_generation.py`(beam search 포함)·`datasets.py`
- **로그**: `lora_experiments.log`(§4.5.1), `multiseed.log`·`module_multiseed.log`(§4.5.2), `lora_hparam_sweep.log`·`alpha_lr_sweep.log`(§4.5.3), `stat_analysis.log`·`learning_curve.log`(§4.5.4), `multiepoch_experiment.log`·`d8000_multiseed.log`·`para_lora16_s42.log`·`para_lora16_s1234.log`(§4.5.5; 참고 `d8000_lr2e4.log`), `sonnet_decode_v2.log`·`sonnet_multiseed.log`·`degeneration.log`(§4.6), `amp_experiment.log`(§4.7), `para_train.log`·`para_lora*.log`(§4.4)
- **예측**: `predictions/para-dev-output.csv`, `para-test-output.csv`, `generated_sonnets.txt`
- **그림**: `figures/fig_*.png` 5종(그림 1–5)
